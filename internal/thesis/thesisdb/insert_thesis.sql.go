// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: insert_thesis.sql

package thesisdb

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const insertThesis = `-- name: InsertThesis :one
INSERT INTO thesis (id, tenant_id, draft_id, piece_profile_key, notification_id, enunciado, forca, estado, created_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, tenant_id, draft_id, piece_profile_key, notification_id, enunciado, forca, estado, created_at
`

type InsertThesisParams struct {
	ID              uuid.UUID          `json:"id"`
	TenantID        uuid.UUID          `json:"tenant_id"`
	DraftID         uuid.UUID          `json:"draft_id"`
	PieceProfileKey *string            `json:"piece_profile_key"`
	NotificationID  pgtype.UUID        `json:"notification_id"`
	Enunciado       string             `json:"enunciado"`
	Forca           string             `json:"forca"`
	Estado          string             `json:"estado"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
}

func (q *Queries) InsertThesis(ctx context.Context, arg InsertThesisParams) (Thesis, error) {
	row := q.db.QueryRow(ctx, insertThesis,
		arg.ID,
		arg.TenantID,
		arg.DraftID,
		arg.PieceProfileKey,
		arg.NotificationID,
		arg.Enunciado,
		arg.Forca,
		arg.Estado,
		arg.CreatedAt,
	)
	var i Thesis
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.DraftID,
		&i.PieceProfileKey,
		&i.NotificationID,
		&i.Enunciado,
		&i.Forca,
		&i.Estado,
		&i.CreatedAt,
	)
	return i, err
}
